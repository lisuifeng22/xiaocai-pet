# 小菜桌宠 — 开发文档

> 项目名称：xiaocai-desktop-pet  
> 版本：v1.0.0  
> 描述：AI 语音陪伴型桌面宠物（Electron + Flask + DeepSeek）

---

## 一、项目概述

小菜桌宠是一个运行在 Windows 桌面上的虚拟宠物应用，基于 Electron 构建透明置顶窗口，通过 SVG 宠物形象 + CSS 状态动画实现桌宠的视觉表现，后端接入 DeepSeek 大模型实现 AI 对话。当前已实现完整的 AI 聊天功能（带对话记忆和宠物个性），支持一键启动。

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面壳 | Electron 33 | 无框透明窗口，始终置顶 |
| 前端 | HTML + CSS + Vanilla JS | 无框架，纯原生实现 |
| 宠物形象 | SVG | 手绘风格，多表情组通过 CSS 切换 |
| 后端 | Flask 3.1 (Python) | `/chat` API，调用 DeepSeek |
| AI 对话 | DeepSeek API | deepseek-chat 模型（OpenAI 兼容接口） |
| 语音 | TTS（规划中） | 豆包 TTS / 其他（待实现） |

---

## 三、项目结构

```
xiaocai-pet/
├── package.json                  # Electron 项目配置 & 构建脚本
├── 启动桌宠.bat                  # 一键启动（后端 + 桌宠）
├── .gitignore
│
├── electron/
│   ├── main.js                   # Electron 主进程（窗口管理、IPC）
│   └── preload.js                # 预加载脚本（安全桥接）
│
├── frontend/
│   ├── index.html                # 主界面（宠物 + 聊天框 + 右键菜单）
│   ├── pet.js                    # 前端逻辑（状态机、拖拽、聊天、番茄钟）
│   ├── style.css                 # 全部样式 & CSS 动画
│   └── assets/
│       └── pet.svg               # 宠物 SVG（含多组表情）
│
├── backend/
│   ├── app.py                    # Flask 主程序（路由 + DeepSeek 调用）
│   ├── config.json               # 后端配置文件
│   ├── .env                      # 环境变量（API Key 等）
│   ├── .env.example              # 环境变量模板
│   ├── requirements.txt          # Python 依赖
│   ├── memory.json               # 宠物状态持久化（好感度/心情/精力）
│   ├── conversations_short.json  # 短期记忆（最近 15 轮对话，文件持久化）
│   └── conversations_long.json   # 长期记忆（超出 15 轮后自动归档的摘要）
│
└── docs/
    └── 2026-05-20-backend-design.md  # 后端设计文档
```

---

## 四、Electron 主进程（electron/main.js）

### 窗口配置
- **窗口尺寸**：280 × 480px（v1.0 从 380px 增高至 480px，为对话气泡留空间）
- **窗口类型**：`frame: false`（无边框）、`transparent: true`（背景透明）
- **置顶层级**：`alwaysOnTop: true` + `screen-saver` 级别（始终在最前）
- **安全隔离**：`contextIsolation: true`、`nodeIntegration: false`

### IPC 通信
| 通道 | 方向 | 功能 |
|------|------|------|
| `drag-window` | Renderer → Main | 接收拖拽偏移量，移动窗口位置 |
| `close-window` | Renderer → Main | 关闭窗口 |

### 预加载脚本（electron/preload.js）
通过 `contextBridge` 暴露 `window.electronAPI` 对象：
- `platform` — 当前平台
- `dragWindow(deltaX, deltaY)` — 窗口拖拽
- `closeWindow()` — 关闭窗口

---

## 五、前端实现（frontend/）

### 5.1 界面布局

```
┌──────────────────────┐
│         Z z Z        │  ← 睡眠指示器
│   ┌──────────────┐   │
│   │  气泡对话内容  │   │  ← 对话气泡（自适应高度，max-width 240px）
│   └──────────────┘   │
│                      │
│     🎀 宠物 SVG     │  ← 核心宠物形象（flex-shrink，可压缩让位）
│                      │
│ [输入框......] [发送] │  ← 聊天输入
│  [专注25分] [休息5分] │  ← 快捷操作
└──────────────────────┘
```

额外：右键点击宠物弹出上下文菜单（喂食 / 说说话 / 设置 / 退出）。

### 5.2 状态机系统

宠物有 6 种状态，每种状态切换对应不同的 CSS 动画和 SVG 表情组合：

| 状态 | 触发条件 | SVG 表情 | CSS 动画 | 说明 |
|------|----------|----------|----------|------|
| `idle` | 默认 / 交互结束恢复 | 正常眼 + 微笑 + 正常眉 | `float` 上下浮动 | 默认待机状态 |
| `happy` | 点击 / 喂食 / 番茄钟结束 | 弯眼笑 + 张嘴笑 + 扬眉 | `bounce` 弹跳（4次） | 开心情绪 |
| `talking` | 聊天 / 说说话 / Alt+R | 正常眼 + 张嘴 + 正常眉 | `talk` 轻微晃动 | 说话中 |
| `angry` | 后端指定 | 怒视眼 + 撇嘴 | `shake` 左右抖动 | 生气情绪 |
| `sleep` | 空闲 45 秒未操作 | 闭眼线 + 微笑 + 放松眉 | `sway` 慢摇 + 半透明 | 睡眠状态 |
| `walk` | 鼠标拖拽中 | 正常眼 + 微笑 + 正常眉 | `walkAnim` 行走抖动 | 被拖动时 |

**状态自动切换逻辑**：
- `idle` → 45 秒无操作 → `sleep`
- `sleep` → 鼠标靠近宠物区域（30px 阈值）→ `idle`
- 交互（点击/喂食/聊天等）→ 切到对应状态 → `setTimeout` 回 `idle`
- 若在 `sleep` 状态下交互，先唤醒到 `idle`

**情绪联动**：后端返回的 `emotion` 字段控制宠物表情切换（happy / talking / idle / angry）。

### 5.3 对话气泡系统
- 一组预设短句 (`BUBBLE_PHRASES`)，随机展示
- 自动消失（4 秒后淡出）
- 后端回复也通过气泡展示
- 气泡带三角形尾巴，半透明白色背景
- **自适应高度**：max-width 240px，无固定高度限制，内容自动撑开
- **窗口弹性布局**：body 使用 flex 列布局，pet-container 为 `flex: 1`，气泡长时自动撑开容器，SVG 通过 `flex-shrink: 1` 压缩让位

### 5.4 拖拽系统
- 鼠标按下宠物 → `walk` 状态 → 实时计算偏移量 → IPC 通知主进程移动窗口
- 鼠标松开 → `idle` 状态

### 5.5 聊天功能
- 输入框 + 发送按钮（也支持 Enter 发送）
- 调用后端 `POST /chat` API，发送 `{ message: "..." }`
- 返回 `{ reply, emotion }`，reply 显示在气泡中，emotion 控制宠物表情
- 错误处理：网络异常时显示"我好像断网了…"

### 5.6 番茄钟
- 通过 HTML onclick 调用 `window.startPomodoro(minutes)`
- 计时结束后弹出提醒
- 目前支持：25 分钟专注 / 5 分钟休息

### 5.7 快捷键
| 快捷键 | 功能 |
|--------|------|
| `Alt+R` | 随机说一句话 |

### 5.8 右键菜单
| 菜单项 | 行为 |
|--------|------|
| 喂食 | 显示"好吃！再来点？" + happy 状态 |
| 说说话 | 随机气泡 + talking 状态 |
| 设置 | 显示"设置还没做好呢 ~" |
| 退出 | 关闭窗口 |

---

## 六、宠物 SVG（frontend/assets/pet.svg）

SVG 手绘风格，尺寸 200×300 viewBox，包含：

### 基础造型
- **发型**：棕色短发 + 齐刘海 + 侧发 + 蝴蝶结发饰（粉色）× 2
- **肤色**：暖调肤色 (`#ffdbca`)
- **服装**：粉色连衣裙 + 白色娃娃领
- **腿脚**：白袜 + 棕色小皮鞋

### 表情组设计（CSS 控制 display 切换）

| 组 | 元素 | 用途 |
|----|------|------|
| `.eye-normal` | 椭圆大眼睛 + 高光 + 下睫毛 | idle / talking / walk |
| `.eye-happy` | 弧形笑眼 | happy |
| `.eye-angry` | 椭圆眼 + 高光 + 下斜粗眉 | angry |
| `.eye-sleepy` | 闭合弧线 + 放松眉 | sleep |
| `.mouth-normal` | 小弧度微笑 | idle / sleep / walk |
| `.mouth-talking` | 张开的小椭圆形嘴 | talking |
| `.mouth-happy` | 大弧度张嘴笑 | happy |
| `.mouth-pout` | 倒弧撅嘴 | angry |
| `.brow-normal` | 平缓自然眉 | 多数状态 |
| `.brow-happy` | 上扬眉 | happy |

---

## 七、样式系统（frontend/style.css）

- **全透明背景**，无边框，`overflow: hidden`
- **字体**：Microsoft YaHei / PingFang SC
- **弹性布局**：`body { display: flex; flex-direction: column; }`，整体纵向排列
- **所有 SVG 表情组**默认 `display: none`，通过 `.state-*` 组合选择器按需显示
- **6 套 CSS 关键帧动画**：float / talk / bounce / shake / sway / walkAnim
- **UI 组件**：气泡 `.bubble`、聊天框 `.chat-box`、快捷按钮 `.quick-actions`、右键菜单 `.context-menu`

---

## 八、后端实现（backend/）

### 架构

```
pet.js (fetch POST /chat) → Flask (127.0.0.1:5000) → DeepSeek API
                                ↕
                           memory.json (个性/好感度持久化)
                           conversations (内存 dict，最近10轮对话记忆)
```

### app.py 核心设计

| 模块 | 说明 |
|------|------|
| `POST /chat` | 接收 `{ message }`，返回 `{ reply, emotion }` |
| `POST /reset` | 清空当前对话历史 |
| `build_system_prompt()` | 从 memory.json 读取 personality/username/favor，动态构建 system prompt |
| `call_llm()` | 调用 DeepSeek API（OpenAI 兼容格式） |
| `parse_emotion()` | 从回复中提取 `[emotion:xxx]` 前缀（支持中文情绪词） |
| `conversations` | 内存 dict，保留最近 10 轮对话 |

### 对话记忆机制
短期记忆 + 长期记忆两级持久化：

**短期记忆**（`conversations_short.json`）：
- 保留最近 15 轮原始对话
- 每次对话后实时持久化到文件
- 随请求加载到 LLM 上下文中，用于多轮对话

**长期记忆**（`conversations_long.json`）：
- 短期记忆超出 15 轮时，自动取出最旧的 5 轮
- 调用 LLM 生成一句话摘要（提取主人喜好、重要事件等）
- 摘要存储到长期记忆文件，旧对话从短期中移除
- 每次对话前将最近 5 条长期记忆注入 system prompt

**记忆流转**：
```text
用户聊天 → 短期记忆(最近15轮) → 超出上限 → LLM总结 → 长期记忆(摘要)
               ↑                                      ↓
         LLM上下文 ← ← ← ← ← ← ← ← ← ← 最近5条摘要注入system prompt
```

### 个性与好感度系统
- System prompt 注入 personality、username、favor
- 好感度分段影响回复语气：冷淡(0-20) / 普通(21-50) / 友好(51-80) / 非常亲近(80+)
- 每次成功对话 `favor += 1`，持久化到 `memory.json`

### 情绪识别
- LLM 回复必须以 `[emotion:xxx]` 开头
- 支持任意情绪词（happy / talking / idle / angry 及中文词如"开心""生气"等）
- 情绪值传给前端控制宠物表情切换

### config.json
```json
{
  "app_name": "小菜桌宠",
  "voice_enabled": true,
  "personality": "嘴毒但关心人",
  "backend_port": 5000
}
```

### .env 配置
| 变量 | 值 | 说明 |
|------|-----|------|
| `DOUBAO_API_KEY` | （空） | 豆包 TTS 密钥（预留） |
| `LLM_API_KEY` | sk-xxx | DeepSeek API 密钥 |
| `LLM_API_URL` | https://api.deepseek.com/v1/chat/completions | DeepSeek 接口地址 |
| `LLM_MODEL` | deepseek-chat | DeepSeek 模型名（实际路由为 deepseek-v4-flash） |

### memory.json（宠物状态持久化）
```json
{
  "username": "主人",
  "personality": "嘴毒但关心人",
  "favor": 0,
  "mood": 70,
  "energy": 80,
  "last_topic": "",
  "today_tasks": []
}
```

### 依赖
- flask 3.1.x
- flask-cors
- python-dotenv
- requests

---

## 九、构建与运行

### 启动方式
1. **一键启动**：双击 `启动桌宠.bat`
   - 自动在后台启动 Flask 后端（等待 3 秒）
   - 自动打开 Electron 宠物窗口
2. **开发运行**：`npm start`（需要先手动 `cd backend && python app.py` 启动后端）

### 启动脚本（启动桌宠.bat）
```
@echo off
cd /d "%~dp0"
start /B /MIN "" cmd /c "cd /d "%~dp0backend" && python app.py"
timeout /t 3 /nobreak >nul
start "" "%CD%\node_modules\electron\dist\electron.exe" "%CD%"
```

### 配置信息
- **appId**: `com.xiaocai.desktop-pet`
- **Windows 构建目标**：NSIS 安装包（允许自定义安装目录）
- **打包包含**：`electron/**/*` + `frontend/**/*`

---

## 十、API 接口

### `POST /chat`

**请求**：
```json
{
  "message": "今天心情怎么样？"
}
```

**响应**：
```json
{
  "reply": "哼，你还知道来找我？我还以为你把我忘在桌角吃灰了呢。",
  "emotion": "talking"
}
```

**错误响应**（后端未运行）：
```json
{
  "reply": "啊？我好像断网了…",
  "emotion": "idle"
}
```

### `GET /history`

查看小菜的完整记忆。

**响应**：
```json
{
  "short_term": [
    "[主人] 你好",
    "[小菜] 哼，你还知道来找我？",
    "[主人] 今天心情怎么样？",
    "[小菜] 心情？被你晾了三天..."
  ],
  "long_term": [
    { "time": "05-20 22:30", "summary": "主人主动来聊天，小菜表现出生气但关心的态度，提到主人有黑眼圈。" }
  ]
}
```

### `POST /reset`

清空对话历史，开始新对话。

**响应**：
```json
{ "status": "ok" }
```

---

## 十一、已实现功能清单

| 功能 | 状态 | 说明 |
|------|------|------|
| ✅ 无框透明桌面窗口 | 已完成 | Electron 透明窗口，始终置顶 |
| ✅ 窗口拖拽移动 | 已完成 | 鼠标按下拖拽移动宠物窗口 |
| ✅ 6 种宠物状态切换 | 已完成 | idle / happy / talking / angry / sleep / walk |
| ✅ SVG 多表情组 | 已完成 | 眼睛、嘴巴、眉毛根据状态切换 |
| ✅ CSS 状态动画 | 已完成 | 每种状态有独立关键帧动画 |
| ✅ 对话气泡（自适应） | 已完成 | 预设随机气泡 + API 回复气泡，自适应高度 |
| ✅ 聊天输入 | 已完成 | 前端 UI + fetch 后端 API |
| ✅ 右键上下文菜单 | 已完成 | 喂食 / 说说话 / 设置 / 退出 |
| ✅ 空闲自动睡眠 | 已完成 | 45 秒无操作进入睡眠 |
| ✅ 鼠标靠近唤醒 | 已完成 | 鼠标移到宠物附近自动唤醒 |
| ✅ 番茄钟 | 已完成 | 25 分钟专注 / 5 分钟休息倒计时 |
| ✅ Alt+R 快捷键 | 已完成 | 随机说话快捷键 |
| ✅ Flask 后端服务器 | 已完成 | 完整实现：路由、LLM 调用、记忆管理 |
| ✅ AI 对话（DeepSeek） | 已完成 | 接入 DeepSeek API，对话带记忆 |
| ✅ 宠物个性注入 | 已完成 | 从 memory.json 读取 personality/favor，动态影响回复 |
| ✅ 好感度系统 | 已完成 | 每次对话 favor+1，持久化到 memory.json |
| ✅ 窗口弹性布局 | 已完成 | 窗口 480px，气泡自适应，SVG 可压缩 |
| ✅ 一键启动 | 已完成 | 双击 bat 自动启动后端 + 前端 |
| ✅ 两级记忆系统 | 已完成 | 短期记忆文件(15轮) + 长期记忆(自动归档摘要) |
| ✅ 记忆查看 API | 已完成 | GET /history 返回短期+长期记忆 |
| ✅ electron-builder 打包配置 | 已完成 | NSIS 安装包构建 |
| ⬜ TTS 语音合成 | 待实现 | 豆包 TTS 或其他引擎 |
| ⬜ 设置页面 | 待实现 | 右键菜单"设置"占位 |
| ⬜ 宠物养成数值联动 | 待实现 | mood / energy 随交互变化 |
| ⬜ 更多交互方式 | 待实现 | 拖拽物品喂食、鼠标抚摸等 |

---

## 十二、开发日志

### v1.0 — 2026-05-20

#### 后端实现
- 创建 `backend/app.py`，实现 Flask 服务器
- `POST /chat` 路由：接收消息 → 构建 system prompt（含个性/好感度）→ 调 DeepSeek API → 解析情绪 → 更新好感度 → 返回回复
- `POST /reset` 路由：清空对话历史
- `GET /history` 路由：查看短期+长期记忆
- 情绪识别：正则提取 `[emotion:xxx]` 前缀，支持中英文情绪词
- 好感度持久化：每次对话 `favor += 1`，写入 `memory.json`
- System prompt 动态构建：根据 favor 分段调整回复语气

#### 两级记忆系统
- 短期记忆 `conversations_short.json`：保留最近 15 轮，实时文件持久化
- 长期记忆 `conversations_long.json`：超出 15 轮后自动将旧对话压缩为摘要归档
- 归档时调用 LLM 提取关键信息（主人喜好、重要事件等）
- 每次对话将最近 5 条长期记忆注入 system prompt
- 短期/长期记忆均可通过 `GET /history` 查看

#### 配置修正
- 创建 `backend/.env`，配置 DeepSeek 标准接口
- `.env.example` 修正为标准 DeepSeek 地址
- `requirements.txt` 修 flask-cors 版本锁

#### 气泡自适应
- Electron 窗口高度 380px → 480px
- `body` 改为 flex 列布局
- `.pet-container` 固定高度 → `min-height: 300px; flex: 1`
- `#svgWrap` 固定高度 → `flex-shrink: 1`，可压缩让位
- 气泡 `max-width`: 200px → 240px，padding 增大

#### 启动优化
- 合并 `启动后端.bat` 和 `启动桌宠.bat` 为单一启动脚本
- 双击 `启动桌宠.bat` 自动启动后端 + 等待 3 秒 + 打开宠物窗口

---

## 十三、后续优先级建议

1. **TTS 语音合成** — 集成豆包 TTS，回复自动朗读
2. **设置页面** — 修改宠物名字、个性、配置 API
3. **宠物养成系统** — mood/energy 随交互动态变化
4. **更多交互** — 拖拽物品喂食、鼠标抚摸等
5. **前端状态增强** — 更多情绪对应的动画和表情
