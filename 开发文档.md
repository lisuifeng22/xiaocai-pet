# 小菜桌宠 — 开发文档

> 项目名称：xiaocai-desktop-pet  
> 版本：v7  
> 描述：AI 语音陪伴型桌面宠物（Electron + Flask + DeepSeek）

---

## 一、项目概述

小菜桌宠是一个运行在 Windows 桌面上的虚拟宠物应用，基于 Electron 构建透明置顶窗口，通过 PNG 宠物形象 + CSS 状态动画实现桌宠的视觉表现，后端接入 DeepSeek 大模型实现 AI 对话。当前已实现完整的 AI 聊天功能（带对话记忆和宠物个性），支持一键启动。v7 已将宠物形象从 SVG 替换为不透明 PNG，视觉更清晰。

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面壳 | Electron 33 | 无框透明窗口，始终置顶 |
| 前端 | HTML + CSS + Vanilla JS | 无框架，纯原生实现 |
| 宠物形象 | PNG（不透明） | 手绘风格，CSS 状态动画 |
| 后端 | Flask 3.1 (Python) | `/chat` API，调用 DeepSeek |
| AI 对话 | DeepSeek API | deepseek-v4-flash 模型 |
| 语音 | TTS（规划中） | 豆包 TTS / 其他（待实现） |

---

## 三、项目结构

```
xiaocai-pet/
├── package.json                  # Electron 项目配置 & 构建脚本
├── 启动桌宠.bat                  # 一键启动（后端 + 桌宠）
├── CLAUDE.md                     # Claude Code 项目规范
├── 开发文档.md                   # 项目开发文档（本文件）
│
├── electron/
│   ├── main.js                   # Electron 主进程（窗口管理、IPC、拖拽边界）
│   └── preload.js                # 预加载脚本（安全桥接）
│
├── frontend/
│   ├── index.html                # 主界面（宠物 + 聊天框 + 右键菜单）
│   ├── pet.js                    # 前端逻辑（状态机、拖拽、聊天、番茄钟）
│   ├── style.css                 # 全部样式 & CSS 动画
│   └── assets/
│       ├── pet.svg               # 旧版 SVG（v6 前使用，现仅作参考）
│       ├── asuka_opaque_body.png # 当前使用的宠物 PNG（不透明）
│       └── asuka_reference_transparent.png # 半透明参考图
│
├── backend/
│   ├── app.py                    # Flask 主程序（路由 + DeepSeek 调用 + 主动推送）
│   ├── config.json               # 后端配置（角色名、个性等）
│   ├── .env                      # 环境变量（API Key 等，勿提交）
│   ├── .env.example              # 环境变量模板
│   ├── requirements.txt          # Python 依赖
│   ├── memory.json               # 宠物状态持久化（好感度/心情/精力）
│   ├── conversations_short.json  # 短期记忆（最近 15 轮对话，文件持久化）
│   └── conversations_long.json   # 长期记忆（超出后自动归档摘要）
│
└── docs/
    └── *.md                      # 设计文档
```

---

## 四、Electron 主进程（electron/main.js）

### 窗口配置
- **窗口尺寸**：280 × 480px（无框透明）
- **置顶层级**：`alwaysOnTop: true` + `screen-saver` 级别
- **安全隔离**：`contextIsolation: true`、`nodeIntegration: false`
- **其他**：`resizable: false`、`hasShadow: false`

### IPC 通信

| 通道 | 方向 | 功能 |
|------|------|------|
| `drag-window-start` | Renderer → Main | 拖拽开始，传入屏幕坐标 + 宠物偏移量 |
| `drag-window-move` | Renderer → Main | 拖拽移动，传入绝对屏幕坐标 |
| `drag-window-end` | Renderer → Main | 拖拽结束 |
| `close-window` | Renderer → Main | 关闭窗口 + 杀死后端进程 |
| `set-window-shape` | Renderer → Main | 设置窗口裁剪形状（矩形数组） |

### 拖拽边界实现（v4 改进）

使用绝对坐标 + 偏移量方案替代增量方案：

```
mousedown: 记录 (startMouseX, startMouseY, startWinX, startWinY, petOffset)
mousemove: 计算 nextX = startWinX + (mouseX - startMouseX)
                     nextY = startWinY + (mouseY - startMouseY)
           然后夹紧：
             topBound    = display.bounds.y - petOffset   （人物顶部贴屏幕顶端）
             bottomBound = workArea.bottom - h             （窗口底部不盖任务栏）
```

相比原来增量方案的优势：
- 消除 IPC 延迟导致的坐标漂移
- 消除因 `getPosition()` 返回过期值引起的抖动
- 单次拖拽中使用同一份 startWinX/startWinY，不存在误差累积

### 窗口裁剪（setShape，v5 新增）

使用 Electron 的 `win.setShape()` 在 OS 层将窗口裁剪为多个矩形区域，裁剪外的区域对系统不存在，鼠标自动穿透：

```
裁剪区域：
┌──────── 人物区 ────────┐   ← 气泡 + PNG + ZZZ 的外包围盒 + 6px padding
│   气泡 (显示时才纳入)    │
│   🧍 PNG (120×144px)   │
└────────────────────────┘
┌──────── 操作栏 ────────┐   ← 聊天输入框 + 快捷按钮 (280×~75px)
│  [输入框] [发送]        │
│  [专注25分] [休息5分]   │
└────────────────────────┘
```

**动态计算逻辑**：
- 默认：仅人物 PNG + 操作栏
- 气泡显示时 → 纳入气泡区域（重新裁剪）
- 气泡消失 → 恢复默认（仅人物）
- ZZZ 睡眠时 → 纳入 ZZZ 区域
- 右键菜单弹出 → 恢复全窗，关闭后重新裁剪
- 拖拽中 → 恢复全窗（避免裁剪导致底部消失），拖拽结束重新裁剪

**预加载桥接**：`setShape(regions)` → `ipcRenderer.send("set-window-shape", regions)`

### 预加载脚本（electron/preload.js）
通过 `contextBridge` 暴露 `window.electronAPI` 对象：
- `platform` — 当前平台
- `dragWindowStart(mouseX, mouseY, petOffset)` — 拖拽开始
- `dragWindowMove(mouseX, mouseY)` — 拖拽移动（绝对坐标）
- `dragWindowEnd()` — 拖拽结束
- `closeWindow()` — 关闭窗口（同时 kill 后端 Python 进程）
- `setShape(regions)` — 设置窗口 OS 级裁剪形状

---

## 五、前端实现（frontend/）

### 5.1 界面布局

```
┌──────────────────────┐
│         Z z Z        │  ← 睡眠指示器（absolute 定位，在 upper-area 内）
│   ┌──────────────┐   │
│   │  气泡对话内容  │   │  ← 气泡（max-width 240px，自适应高度）
│   └──────────────┘   │
│     ┌──────────┐     │
│     │ 🎀 宠物  │     │  ← pet-container (fit-content), 仅包裹 PNG
│     └──────────┘     │
├──────────────────────┤  ← 窗口裁剪边界（setShape 人物区）
│ [输入框......] [发送] │  ← 聊天输入（固定 208px + 50px）
│  [专注25分] [休息5分] │  ← 快捷操作
└──────────────────────┘  ← 窗口裁剪边界（setShape 操作栏）
```

额外：右键点击宠物弹出上下文菜单（喂食 / 说说话 / 设置 / 退出）。

### 5.2 宠物形象加载

v7 改为使用不透明 PNG 而非 SVG。`pet.js` 的 `mountPetImage()` 在 `#svgWrap` 内动态创建 `<img>` 标签加载 `asuka_opaque_body.png`，不再依赖 SVG 内联表情组。

```js
// mountPetImage() 生成的 DOM：
<img class="pet-img" src="./assets/asuka_opaque_body.png" draggable="false" alt="小菜" />
```

状态切换改为在 `#svgWrap` 上切换 `state-*` class，CSS 通过 `.pet-img` 选择器应用对应动画。由于是静态 PNG，表情切换通过不同图片文件实现（当前仅一张图，无表情切换，状态仅体现为动画不同）。

### 5.3 状态机系统

宠物有 6 种状态，每种状态对应不同的 CSS 动画：

| 状态 | 触发条件 | CSS 动画 | 说明 |
|------|----------|----------|------|
| `idle` | 默认 / 交互结束恢复 | `float` 上下浮动 | 默认待机 |
| `happy` | 点击 / 喂食 / 番茄钟结束 | `bounce` 弹跳（4次） | 开心情绪 |
| `talking` | 聊天 / 说说话 / Alt+R | `talk` 轻微晃动 | 说话中 |
| `angry` | 后端指定 | `shake` 左右抖动 | 生气情绪 |
| `sleep` | 空闲 45 秒未操作 | `sway` 慢摇 | 睡眠状态 |
| `walk` | 鼠标拖拽中 | **无动画（静态）** | 被拖动时 |

**状态自动切换逻辑**：
- `idle` → 45 秒无操作 → `sleep`
- `sleep` → 鼠标靠近宠物区域（30px 阈值）→ `idle`
- 交互（点击/喂食/聊天等）→ 切到对应状态 → `setTimeout` 回 `idle`
- 若在 `sleep` 状态下交互，先唤醒到 `idle`
- 窗口失去焦点（`window.blur`）→ 强制结束拖拽

### 5.4 对话气泡系统
- 一组预设短句 (`BUBBLE_PHRASES`)，随机展示
- 自动消失（4 秒后淡出）
- 后端回复也通过气泡展示
- 气泡带三角形尾巴，半透明白色背景
- **自适应高度**：max-width 240px，无固定高度限制
- **窗口弹性布局**：body 使用 flex 列布局，upper-area 填充空间，pet-container 为 `fit-content`

### 5.5 拖拽系统（v4 重构）

**方案**：绝对坐标 + requestAnimationFrame 节流

- 鼠标按下 → `e.screenX/Y` 记录初始屏幕坐标 → `dragWindowStart` IPC
- 鼠标移动 → rAF 节流 → `dragWindowMove` IPC（携带最新屏幕坐标）
- 主进程用 `startWinXY + (currentMouseXY - startMouseXY)` 计算窗口目标位置
- 鼠标松开 / 窗口失焦 → `dragWindowEnd` IPC

**防抖修复**：
- `wasDragged` 标记（鼠标偏移 > 3px）→ 阻止拖拽后的 `click` 误触发
- rAF 节流 → 避免高频 IPC + `setPosition` 抖动
- 拖拽时 body 添加 `is-dragging` class → 全局 `cursor: grabbing`
- `window.blur` 事件 → 防止拖拽时切窗口导致鼠标卡住

### 5.6 聊天功能
- 输入框 + 发送按钮（Enter 发送）
- 调用后端 `POST /chat` API
- 错误处理：网络异常时显示"网络断了？真麻烦。"

### 5.7 番茄钟
- 通过 HTML onclick 调用 `window.startPomodoro(minutes)`
- 支持：25 分钟专注 / 5 分钟休息

### 5.8 快捷键
| 快捷键 | 功能 |
|--------|------|
| `Alt+R` | 随机说一句话 |

### 5.9 右键菜单
| 菜单项 | 行为 |
|--------|------|
| 喂食 | 显示"哼……还算合格。" + happy 状态 |
| 说说话 | 随机气泡 + talking 状态 |
| 设置 | 显示"设置还没做好，别催。" |
| 退出 | 关闭窗口 + 杀死后端 Python 进程 |

### 5.10 SSE 实时消息（半实现）
前端 `pet.js` 包含 `EventSource` 连接到 `http://localhost:5000/stream` 的代码，用于接收后端主动推送消息。但后端 `/stream` 路由未实现，`send_message_to_frontend()` 当前仅为 `print`。

---

## 六、样式系统（frontend/style.css）

- **全透明背景**，无边框，`overflow: hidden`
- **字体**：Microsoft YaHei / PingFang SC
- **弹性布局**：`body { display: flex; flex-direction: column; }`
- **6 套 CSS 关键帧动画**：float / talk / bounce / shake / sway
- **walk 状态无动画**（`animation: none !important; transform: translateY(0)`）
- **宠物图片动画**通过 `.pet-img` 选择器独立定义（idle/talking/happy/angry/sleep/walk）
- **性能优化**：PNG 添加 `will-change: transform`
- **拖拽光标**：body 添加 `.is-dragging` class → 全局 `cursor: grabbing`
- **背景板光标**：`cursor: grab` 仅设置在 `#svgWrap` 上，非宠物区域不显示手掌

### CSS 尺寸硬锁定（v4 修复）
- `html, body` 宽度锁定 280px
- `.chat-box` 宽度 280px
- `input#userInput` 固定 208px
- `button#sendBtn` 固定 50px
- 拖拽时禁用 transition 避免抖动

### SVG 遗留样式
CSS 中仍保留 SVG 表情组选择器（`.eye-group`、`.mouth-group`、`.brow-*`），当前未被使用（未加载 SVG）。后续若切回 SVG 可复用。

### CSS 尺寸
| 元素 | 尺寸 |
|------|------|
| 窗口 | 280 × 480px |
| upper-area | 弹性填充窗口上部 |
| pet-container | `fit-content`（大约 120 × 144px） |
| 宠物图片 | 120 × 144px |
| 气泡 | max-width 240px |
| 输入框 | 208px（flex 锁定 `0 0 208px`） |
| 发送按钮 | 50px |

---

## 七、后端实现（backend/）

### 架构

```
pet.js (fetch POST /chat) → Flask (127.0.0.1:5000) → DeepSeek API
                                ↕
                           memory.json (个性/好感度持久化)
                           conversations_short.json (最近15轮对话)
                           conversations_long.json (摘要归档)
```

### app.py 核心模块

| 模块 | 说明 |
|------|------|
| `POST /chat` | 接收 `{ message }`，返回 `{ reply, emotion }` |
| `GET /history` | 查看短期 + 长期记忆 |
| `POST /reset` | 清空对话历史 |
| `POST /focus_done` | 番茄钟完成回调，触发主动提醒 |
| `build_system_prompt()` | 从 memory.json 构建 system prompt（含个性、好感度、长期记忆、当前时间） |
| `call_llm()` | 调用 DeepSeek API |
| `parse_emotion()` | 正则提取 `[emotion:xxx]` 前缀 |
| `strip_emotion_prefix()` | 移除 emotion 标签，返回纯文本 |
| `append_short_term()` | 持久化短期记忆 + 超限自动归档 |
| `proactive_loop()` | 后台线程：定时问候 + 闲置太久提醒 |

### 主动推送系统（v6 新增）

**定时问候**（后台线程 `proactive_loop`）：
- 08:00 → "早上好，今天也要努力哦~"
- 12:00 → "中午啦，吃饭了吗？"
- 21:00 → "晚安时间到了，该休息啦~"
- 每天只触发一次，日期变更自动重置

**闲置提醒**：
- 1 小时无交互（`last_interaction` 超过 3600 秒）→ 提醒
- 每隔 30 秒检查一次

**推送方式**：
- 当前通过 `print()` 输出到控制台（SSE 流 `/stream` 端点未实现）

### 对话记忆机制

**短期记忆**（`conversations_short.json`）：
- 保留最近 15 轮对话
- 每次对话后实时持久化到文件
- 超出 15 轮时，自动取出最旧的 5 轮归档

**长期记忆**（`conversations_long.json`）：
- 归档时调用 LLM 生成一句话摘要
- 摘要存储到长期记忆文件
- 每次对话前将最近 5 条长期记忆注入 system prompt

### 个性与好感度系统

- config.json 配置角色名和个性描述（傲娇风格）
- 好感度分段：冷淡(0-20) / 普通(21-50) / 友好(51-80) / 非常亲近(80+)
- 每次成功对话 `favor += 1`，持久化到 memory.json
- 当前好感度：53（友好）

### config.json
```json
{
  "app_name": "Asuka Mode 桌宠",
  "personality": "傲娇、嘴硬、行动力强，表面嫌弃主人，但会认真提醒主人学习、工作和休息...",
  "voice_enabled": true,
  "auto_speak": true,
  "backend_port": 5000
}
```

### memory.json
```json
{
  "username": "主人",
  "favor": 53,
  "mood": 75,
  "energy": 90,
  "last_topic": "",
  "today_tasks": []
}
```

### 对话增强（v6）
- System prompt 要求回复以 `[emotion:xxx]` 开头标记情绪
- 鼓励在回复中使用 emoji（如 😏 😡 🥺 ✨ 🎉 💤 🤔 💪 👀 ❤️）
- 旧版冗余 `[emotion:xxx]` 标签会在回复中被清理
- 注入当前时间，模型可准确回答日期和星期
- temperature 0.9 保证回复自然随机

---

## 八、构建与运行

### 启动方式
1. **一键启动**：双击 `启动桌宠.bat`
   - 自动在后台启动 Flask 后端（等待 3 秒）
   - 自动打开 Electron 宠物窗口
2. **开发运行**：`npm start`（需先手动启动后端 `cd backend && python app.py`）

### 启动脚本（启动桌宠.bat）
```
@echo off
chcp 65001 >nul
cd /d "%~dp0"
start /B /MIN "" cmd /c "cd /d "%~dp0backend" && python app.py"
timeout /t 3 /nobreak >nul
start "" "%CD%\node_modules\electron\dist\electron.exe" "%CD%"
exit
```

### 依赖
**前端**：
- `electron ^33.0.0`
- `electron-builder ^25.0.0`

**后端**（`requirements.txt`）：
- `flask 3.1.x`
- `flask-cors`
- `python-dotenv 1.x`
- `requests 2.x`

---

## 九、API 接口

### `POST /chat`

**请求**：`{ "message": "今天心情怎么样？" }`
**响应**：`{ "reply": "...", "emotion": "talking" }`

### `GET /history`
返回短期 + 长期记忆。

### `POST /reset`
清空对话历史。

### `POST /focus_done`
番茄钟完成回调，触发主动推送。

---

## 十、已实现功能清单

| 功能 | 状态 | 说明 |
|------|------|------|
| ✅ 无框透明桌面窗口 | 已完成 | Electron 透明窗口，始终置顶 |
| ✅ 窗口拖拽移动 | 已完成 | 绝对坐标 + rAF 节流，边界限制 |
| ✅ 顶部边界夹紧 | 已完成 | 以人物视觉顶部为参考贴屏幕顶端 |
| ✅ 底部避让任务栏 | 已完成 | 窗口底部不超出任务栏 |
| ✅ 6 种宠物状态切换 | 已完成 | idle / happy / talking / angry / sleep / walk |
| ✅ CSS 状态动画 | 已完成 | 每种状态有独立关键帧动画 |
| ✅ 对话气泡（自适应） | 已完成 | 预设气泡 + API 回复，自适应高度 |
| ✅ 聊天输入 | 已完成 | 前端 UI + fetch 后端 API |
| ✅ 右键上下文菜单 | 已完成 | 喂食 / 说说话 / 设置 / 退出 |
| ✅ 空闲自动睡眠 | 已完成 | 45 秒无操作进入睡眠 |
| ✅ 鼠标靠近唤醒 | 已完成 | 鼠标移到宠物附近自动唤醒 |
| ✅ 番茄钟 | 已完成 | 25 分钟专注 / 5 分钟休息 |
| ✅ Alt+R 快捷键 | 已完成 | 随机说话 |
| ✅ Flask 后端服务器 | 已完成 | 路由、LLM 调用、记忆管理 |
| ✅ AI 对话（DeepSeek） | 已完成 | 对话带记忆，个性注入，emoji 回复 |
| ✅ 好感度系统 | 已完成 | 每次对话 favor+1，持久化 |
| ✅ 两级记忆系统 | 已完成 | 短期文件(15轮) + 长期摘要归档 |
| ✅ 明日香主题形象 | 已完成 | 傲娇风格角色 |
| ✅ 拖拽防抖修复 | 已完成 | wasDragged + rAF 节流 + blur 结束 |
| ✅ 输入框宽度锁定 | 已完成 | flex: none + calc(100% - 56px) |
| ✅ 时间感知注入 | 已完成 | system prompt 注入当前时间，模型可准确回答日期 |
| ✅ 背景板光标修复 | 已完成 | cursor:grab 限制在 #svgWrap，非宠物区域不显示手掌 |
| ✅ 窗口裁剪（setShape） | 已完成 | OS 级裁剪，仅保留人物区 + 操作栏，空白区域鼠标穿透 |
| ✅ 拖拽恢复全窗 | 已完成 | 拖拽时 setShape([]) 恢复完整窗口，结束后重新裁剪 |
| ✅ 退出杀后端 | 已完成 | 右键退出或 Alt+F4 时同时 taskkill Python 进程 |
| ✅ 不透明 PNG 宠物形象 | 已完成 | 替换 SVG 为不透明 PNG，消除白色描边残留 |
| ✅ 定时问候 & 闲置提醒 | 已完成 | 后台线程：早中晚问候 + 1 小时闲置提醒（输出待完善） |
| ✅ Emoji 情绪回复 | 已完成 | System prompt 要求回复带 emoji，更生动 |
| ⬜ TTS 语音合成 | 待实现 | 豆包 TTS 或其他引擎 |
| ⬜ 设置页面 | 待实现 | 右键菜单"设置"占位 |
| ⬜ 宠物养成数值联动 | 待实现 | mood / energy 随交互变化 |
| ⬜ 更多交互方式 | 待实现 | 拖拽物品喂食、鼠标抚摸等 |
| ⬜ SSE 推送端点 | 待实现 | 后端 `/stream` 路由 + 前端 `displayPetMessage` |

---

## 十一、开发日志

### v7 — 2026-05-23

#### 不透明 PNG 替换
- 用 `asuka_opaque_body.png` 替换内联 SVG 作为宠物形象
- `pet.js` 新增 `mountPetImage()`，在 `#svgWrap` 中动态创建 `<img>` 标签
- CSS 新增 `.pet-img` 选择器组，为每种状态定义独立动画
- 移除 SVG 依赖，不再需要内联表情组（眼睛/嘴巴/眉毛）的切换逻辑

#### 白色描边消除
- 优化 PNG 边缘透明度，消除白色描边残留
- 移除宠物图片周围的白色点状像素，保留黑色线条轮廓

#### 面部和颜色优化
- 调整 PNG 面部特征和配色，提升视觉表现

### v6 — 2026-05-22

#### Emoji 情绪回复
- System prompt 要求回复以 `[emotion:xxx]` 开头标记情绪
- 鼓励回复中使用 emoji 表达情绪（😏 😡 🥺 ✨ 🎉 💤 🤔 💪 👀 ❤️）
- 前端展示时通过 `strip_emotion_prefix()` 清理 emotion 标签
- 旧版聊天记录中包含的多余 emotion 标签被清理

#### 模型切换
- 默认模型从 `deepseek-chat` 切换为 `deepseek-v4-flash`

#### 主动推送系统
- 新增 `proactive_loop()` 后台线程（daemon）
- 定时问候：08:00 / 12:00 / 21:00（每天仅触发一次）
- 闲置提醒：1 小时无交互时触发
- 新增 `POST /focus_done` 端点，番茄钟完成回调
- 新增 `last_interaction` 全局变量跟踪最后交互时间
- 推送方式当前仅为 `print()`，SSE 流待实现

#### 代码整理
- `app.py` 和 `pet.js` 代码排版整理，模块化分组
- 常量提取（`SHORT_MAX_ROUNDS`、`ARCHIVE_BATCH`、`INACTIVITY_THRESHOLD` 等）

### v5 — 2026-05-21

#### 背景板光标修复
- 将 `cursor: grab` 从 `.pet-container` 移至 `#svgWrap`，只有悬停宠物 SVG 才显示手掌光标
- 背景板空白区域恢复默认箭头光标

#### 布局重构
- 新增 `.upper-area` 容器，取代 `.pet-container` 的弹性填充角色
- 气泡和 ZZZ 移出 `.pet-container`，作为 `.upper-area` 的直接子元素
- `.pet-container` 改为 `width: fit-content; height: fit-content;` 仅包裹 SVG
- 背景板从 480px 全高缩小至约 120×144px

#### 窗口裁剪（setShape）
- 引入 Electron 的 `win.setShape()` 在 OS 层裁剪窗口
- 裁剪为两个矩形区域：人物区（SVG + 气泡 + ZZZ）+ 操作栏（输入框 + 快捷按钮）
- 裁剪区域外的鼠标事件自动穿透到桌面
- 默认人物区仅包含 SVG，气泡只在显示时纳入
- 右键菜单弹出时恢复全窗，关闭后重新裁剪

#### 拖拽体验优化
- 拖拽时调用 `setShape([])` 恢复全窗，避免裁剪导致底部操作栏消失
- 内部 `requestAnimationFrame` 回调通过 `isDragging` 标志跳过形状更新
- 拖拽结束后 `setPetState("idle")` 自动重新裁剪

#### 退出时杀后端
- `close-window` IPC 处理函数中添加 `exec("taskkill /F /IM python.exe")`
- 窗口 `closed` 事件同样触发后端进程终止
- 无论右键退出还是 Alt+F4 关闭，后端 Python 进程都会清理

### v4 — 2026-05-21

#### 时间感知注入
- 在 `build_system_prompt()` 末尾注入 `datetime.now()` 格式化的当前时间
- 每次对话 DeepSeek 都知道准确日期和时间，能回答"今天几号""星期几"
- 0.9 temperature + 傲娇性格保证回复自然随机，不机械

#### 拖拽系统重构
- 从增量方案改为绝对坐标方案：renderer 发送 `e.screenX/Y`，main 进程用 `startWinXY + (currentMouseXY - startMouseXY)` 计算位置
- 引入 `requestAnimationFrame` 节流，避免高频 IPC + setPosition 抖动
- 添加 `window.blur` 事件处理，防止拖拽时切换窗口导致鼠标卡住
- wasDragged 标记（鼠标偏移 > 3px），阻止拖拽后 click 事件误触发

#### 拖拽边界实现
- 顶部：`display.bounds.y - petOffset`（人物视觉顶部贴屏幕顶端）
- 底部：`display.workArea.y + display.workArea.height - h`（窗口底部不盖任务栏）
- petOffset 在 mousedown 时通过 `getBoundingClientRect()` 实时计算

#### CSS 修复
- `state-walk` 取消 `transform` 动画 → 改为 `animation: none; transform: translateY(0)`
- SVG 添加 `will-change: transform` 性能优化
- 拖拽时 body 添加 `is-dragging` class 实现全局 `cursor: grabbing`
- 输入框改用 `flex: none; width: calc(100% - 56px); box-sizing: border-box` 消除 flex 波动
- 发送按钮固定宽度 50px

#### 样式格式化
- 多行 CSS 格式化，提高可读性
- 选择器拆分（`.chat-box button` 与 `.quick-actions button` 分离）

### v3 — 2026-05-21

#### 边界修复
- 使用 `screen.getDisplayNearestPoint()` 获取当前显示器
- 添加 `try-catch` 防止错误弹窗
- 添加 `typeof` 类型校验，防止 NaN/undefined

### v2 — 2026-05-21

#### 宠物形象更新
- 宠物 SVG 从通用猫娘替换为明日香主题（内嵌 PNG base64）
- 更新 config.json 角色名为 "Asuka Mode 桌宠"
- 更新 personality 为明日香风格：傲娇、嘴硬、行动力强
- CSS 添加 Asuka character patch（`#svgWrap` 尺寸调整为 120×144px）

### v1 — 2026-05-20

#### 初始版本
- 创建 Electron + Flask 项目结构
- 实现基础拖拽、状态机、对话系统
- 实现两级记忆系统
- 实现好感度系统
- 实现番茄钟
- 一键启动脚本
- electron-builder 打包配置
